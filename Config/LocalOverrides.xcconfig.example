// Optional local build inputs.
// Copy this file to LocalOverrides.xcconfig and set a local path if needed.
// The file is optional and is not required for building the project.
AUXILIARY_RESOURCE_ROOT =
